{
    "builds": [{
        "src": "api/app.py",
        "use": "@vercel/python",
        "config": {
            "python": "3.10"
        }
    }],
    "rewrites": [
        { "source": "/(.*)", "destination": "/api/app" }
    ]
}